# Simulates a conf file that doesn't contain the VERSION_KEY at all
# (e.g. argfile-bootc-cuda-disk-image-container.conf).
# The script should still check for EA/fast patterns.
OCI_ARTIFACT=quay.io/redhat-user-workloads/rhel-ai-tenant/rhelai-bootc/bootc-cuda-qcow2-disk-image@sha256:abc123
OCI_EXTRACT_IMAGE=registry.redhat.io/ubi9/ubi:9.5-1745854298
IMAGE_PULL_SECRET=bootc-cuda-qcow2-disk-image-image-pull
